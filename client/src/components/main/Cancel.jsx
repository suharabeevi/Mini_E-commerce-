import React from 'react';

const Cancel = () => {
    const CancelStyle = {
        backgroundColor: 'red',
        color: 'white',
        padding: '10px',
        borderRadius: '5px',
        textAlign: 'center'
    };

    return (
        <div style={CancelStyle}>
            Cancel
        </div>
    );
}

export default Cancel;
